{
  "name": "json-api",
  "version": "1.0.0",
  "description": "json document stored API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --exec 'npm run lint && node'",
    "lint": "npx eslint --ext .ts,.js,.vue --ignore-path .gitignore .",
    "test": "jest"
  },
  "author": "CapocciFlorent",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^17.1.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mysql2": "^2.1.0",
    "sequelize": "^5.21.5",
    "swagger-ui-express": "^4.1.3"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "jest": "^25.1.0",
    "nodemon": "^2.0.2"
  }
}
